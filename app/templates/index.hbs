<div class="index-header">
  <img src="" alt="mountains" />
</div>
<div class="index-body">
  <div class="row">
    {{#each model as |post|}}
      {{post-tile post=post}}
    {{/each}}
  </div>
</div>
